<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- ==================== Permissions ==================== -->

    <!-- Réseau -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <!-- Caméra (vérification d'âge) -->
    <uses-permission android:name="android.permission.CAMERA" />

    <!-- Micro (recherche vocale) -->
    <uses-permission android:name="android.permission.RECORD_AUDIO" />

    <!-- Localisation (Google Maps - cinémas proches) -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />

    <!-- ==================== Fonctionnalités matérielles ==================== -->

    <uses-feature
        android:name="android.hardware.camera"
        android:required="false" />
    <uses-feature
        android:name="android.hardware.camera.front"
        android:required="false" />
    <uses-feature
        android:name="android.hardware.microphone"
        android:required="false" />
    <uses-feature
        android:name="android.hardware.location.gps"
        android:required="false" />
    <uses-feature
        android:name="android.hardware.location.network"
        android:required="false" />

    <!-- ==================== Application ==================== -->

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.La7da"
        android:usesCleartextTraffic="true"
        tools:targetApi="34">

        <!-- ==================== Clé API Google Maps ==================== -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="AIzaSyDkm_pxNcxgH4DkMz3BBwF7uPnXw3KGnMo" />

        <!-- ============ Écran d'introduction (Lanceur) ============ -->
        <activity
            android:name=".Activity.IntroActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- ============ Écran de connexion ============ -->
        <activity
            android:name=".Activity.LoginActivity"
            android:exported="false"
            android:screenOrientation="portrait"
            android:windowSoftInputMode="adjustResize" />

        <!-- ============ Mot de passe oublié ============ -->
        <activity
            android:name=".Activity.ForgotPasswordActivity"
            android:exported="false"
            android:screenOrientation="portrait"
            android:windowSoftInputMode="adjustResize" />

        <!-- ============ Inscription ============ -->
        <activity
            android:name=".Activity.RegisterActivity"
            android:exported="false"
            android:screenOrientation="portrait"
            android:windowSoftInputMode="adjustResize" />

        <!-- ============ Écran principal (liste des films) ============ -->
        <activity
            android:name=".Activity.MainActivity"
            android:exported="false"
            android:screenOrientation="portrait"
            android:windowSoftInputMode="adjustResize"
            android:launchMode="singleTop" />

        <!-- ============ Détails d'un film ============ -->
        <activity
            android:name=".Activity.DetailActivity"
            android:exported="false"
            android:screenOrientation="portrait" />

        <!-- ============ ChatBot AI (Gemini) ============ -->
        <activity
            android:name=".Activity.ChatBotActivity"
            android:exported="false"
            android:screenOrientation="portrait"
            android:configChanges="orientation|screenSize|keyboardHidden"
            android:windowSoftInputMode="adjustResize" />

        <!-- ============ Lecteur vidéo (bandes-annonces) ============ -->
        <activity
            android:name=".Activity.VideoPlayer"
            android:exported="false"
            android:configChanges="orientation|screenSize|keyboardHidden"
            android:screenOrientation="sensorLandscape" />

        <!-- Profile -->
        <activity android:name=".Activity.ProfileActivity" android:exported="false" />
        <!-- Purchased Movies -->
        <activity android:name=".Activity.PurchasedMoviesActivity" android:exported="false" />
        <!-- Favorite Movies -->
        <activity android:name=".Activity.FavoriteMoviesActivity" android:exported="false" />

    </application>
</manifest>